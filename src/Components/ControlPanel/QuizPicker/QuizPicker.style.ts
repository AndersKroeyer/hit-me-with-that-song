import { css } from "@emotion/react";

const styles = css({
  toggleButton: {
    width: '250px',
    height: '100px',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    fontSize: '30px',
    margin: '2px',
  },
});

export default styles;
