import { css } from "@emotion/react";

const styles = {
  container: css({
    display: 'flex',
    flexDirection: 'column',
    marginBottom: '50px',
    alignItems: 'center',
    padding: '10px',
  }),
  teamName: css({
    fontSize: '22px',
  }),
};

export default styles;
